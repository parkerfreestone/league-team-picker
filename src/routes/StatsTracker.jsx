import React from "react";
import { Container } from "react-bootstrap";

const StatsTracker = (props) => {
    return (
        <Container
            className="mt-5"
        >
            <h1>Stats Tracker</h1>
            <p class="lead">Custom League of Legends Games</p>
        </Container>
    );
}

export default StatsTracker;